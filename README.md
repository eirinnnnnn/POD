# Polar Orbit Decoding

The original data of the plotted curves are stored in the folder project/POD

m and t are the parameters for eBCH $n=2^m$ with designed minimum distance $d = 2t+1$, stored in the same format as TT's log files

before naming it as POD I called it AED :p

in the folder of each code, a python script plot_logs.py can generate a quick plot by accessing the log folders with the legend named after the folder's name. run 
```
python3 plot_logs.py -h
```
to see the available arguments.

text me if you found anything suspesious ouob